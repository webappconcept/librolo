// app/(admin)/admin/permissions/page.tsx — aggiunto metadata
import type { Metadata } from "next";
export { default } from "./permissions-page";
export const metadata: Metadata = { title: "Permessi" };
