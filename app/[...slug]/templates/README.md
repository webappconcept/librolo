# DEPRECATO

Questa cartella è stata sostituita da `app/(frontend)/_templates/`.

Vedere:
- `app/(frontend)/_templates/registry.ts` per la mappatura slug → componente
- `app/(frontend)/[...slug]/page.tsx` per il routing CMS
